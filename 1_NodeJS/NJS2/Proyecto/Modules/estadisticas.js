export function promedio(a, b, c) {
    return (Number(a) + Number(b) + Number(c)) / 3;
}


export function mayor(a, b, c) {
    return Math.max(Number(a), Number(b), Number(c));
}


export function menor(a, b, c) {
    return Math.min(Number(a), Number(b), Number(c));
}